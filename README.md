# Spine Anomaly Classification

Проект посвящён задаче бинарной классификации аномалий позвоночника на основе биомеханических параметров таза и позвоночного столба. Работа выполнена как практический ML‑кейс с фокусом на корректный EDA, интерпретируемые модели и медицинский контекст.

---

## 📊 Описание данных

Используется табличный датасет из **310 наблюдений** с шестью числовыми признаками и одной целевой переменной:

**Признаки:**
- `pelvic_incidence` — тазовый угол
- `pelvic_tilt` — наклон таза
- `lumbar_lordosis_angle` — угол поясничного лордоза
- `sacral_slope` — наклон крестца
- `pelvic_radius` — радиус таза
- `grade_of_spondylolisthesis` — степень спондилолистеза

**Целевая переменная:**
- `Class_att`: `Normal` / `Abnormal`

Данные не содержат пропусков, но характеризуются:
- выраженной асимметрией распределений,
- наличием выбросов (медицински значимых),
- мультиколлинеарностью между анатомическими признаками.

---

## 🔍 Exploratory Data Analysis (EDA)

В ходе EDA были выполнены:
- анализ описательных статистик,
- сравнение распределений по целевому классу,
- визуализация гистограмм и boxplot с разделением по классам,
- анализ корреляций и тепловая карта признаков,
- оценка мультиколлинеарности с помощью VIF.

EDA показал, что большинство признаков имеют **различимые распределения** для классов `Normal` и `Abnormal`, а данные в целом обладают **почти линейной разделимостью**.

---

## 🤖 Модели машинного обучения

В проекте были обучены и сравнены две модели:

### 1️⃣ Logistic Regression (основная модель)

Использовался пайплайн:
- логарифмическое преобразование асимметричных признаков,
- `RobustScaler` для устойчивости к выбросам,
- `LogisticRegression` с учётом дисбаланса классов.

**Результаты:**
- ROC-AUC: **0.86**
- Accuracy: **0.85**
- Высокая точность и полнота для класса «аномалия»

Модель показала наилучшее обобщающее качество и высокую интерпретируемость.

---

### 2️⃣ Random Forest (бенчмарк)

Использовался как нелинейная ансамблевая модель для сравнения.

**Результаты:**
- ROC-AUC: **0.72**
- Accuracy: **0.79**

Модель показала более низкое качество, вероятно, из-за небольшого размера выборки и высокой коррелированности признаков.

---

## 📈 Сравнение моделей

| Метрика | Logistic Regression | Random Forest |
|------|---------------------|---------------|
| ROC-AUC | **0.86** | 0.72 |
| Accuracy | **0.85** | 0.79 |
| Интерпретируемость | Высокая | Низкая |

Логистическая регрессия превзошла Random Forest по ключевым метрикам качества.

---

## ✅ Выводы

- Данные обладают преимущественно линейной структурой.
- Корректная предобработка и масштабирование критичны для качества модели.
- Простая интерпретируемая модель может превосходить более сложные алгоритмы.

---

## 🧭 Рекомендации

- Использовать Logistic Regression как основную модель для медицинского скрининга.
- Применять Random Forest как вспомогательный бенчмарк.
- Для дальнейшего развития проекта:
  - расширить датасет,
  - провести кросс-валидацию,
  - интерпретировать коэффициенты модели,
  - протестировать модель на внешних данных.

---

## 🛠 Используемые технологии

- Python
- NumPy, Pandas
- Matplotlib, Seaborn
- Scikit-learn
- Statsmodels

## Данные:
https://www.kaggle.com/datasets/jessanrod3/vertebralcolumndataset

